# Progresso — Backapeando

## Status atual

- Estado: EM DESENVOLVIMENTO
- Última atualização: 2026-09-16 (Dashboard: gráficos de backups e dados por destino)
- Branch atual: `main` (única branch, sem remote)

## Em andamento

- [x] GitHub Actions para publicar `docs/arquitetura.html` no GitHub Pages (concluída).

## Concluído

| Data       | Tarefa                                                                                                                         | Plano                                                                                                                                                                                  | Rodadas | Validator                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Tester                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Segurança                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Regras                                                                                               | Documentos                                                                                                                                                                                                                                                                                                                                                                                                      | Observações                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-15 | Inicialização da governança (`/init-project --update`)                                                                         | `Backapeando-2026-09-15-17-41-inicializacao-governanca.md`                                                                                                                             |       1 | n/a (execução direta, sem Coder/Validator separados — tarefa de documentação)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | n/a                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | RN-BACKUP-001 a 015, RN-STORAGE-001, RN-AUTH-001 (todas ⚠ inferida)                                  | RegrasNegocio.md (novo), Arquitetura.md (novo), Organograma.md (novo), Infraestrutura.md (novo), API.md (novo), Frontend.md (novo), Auth.md (novo), RAG.md (novo), Harness.md (novo), Memoria.md (novo), CLAUDE.md (atualizado)                                                                                                                                                                                 | `docs/` estava inteiramente ausente apesar do `CLAUDE.md` já referenciá-lo; corrigida divergência `azure_targets`→`storage_targets` e cookie de sessão hardcoded→configurável                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-09-15 | CRUD de usuário administrador (via web) + correção de bug na tela de Histórico                                                 | `Backapeando-2026-09-15-18-16-crud-admin-users-e-fix-historico.md`                                                                                                                     |       2 | Rodada 1: REJECTED — faltavam testes de backend para `admin_users` (só existia teste de `backup_runs`), `.gitignore`/`backend/coverage.out` alterados sem autorização por um subagente de revisão (revertidos), frase stale em `RegrasNegocio.md`. Rodada 2: APPROVED após `backend/internal/httpapi/handlers/admin_users_test.go` (15 testes unitários passando + 8 de integração com `t.Skip` limpo sem `DATABASE_URL`) e correções de documentação                                                                                                                                                                                                                                                                                                           | Frontend: 247/247 testes, lint e build limpos. Backend: `go build`/`go vet`/`gofmt` limpos, 164+15 testes unitários passando; testes de integração (`repository`, `httpapi`, incluindo os novos de `admin_users` e `backup_runs`) requerem `DATABASE_URL`, não executáveis nesta sessão (permissão bloqueou leitura de `.env`, mesmo indireta)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | APPROVED (0 críticos/altos; 2 baixos: corrida não-atômica no bloqueio de último-admin — risco aceito, ver ADR-004 — e log de erro do Axios em dev que podia incluir senha em texto puro no payload — corrigido nesta mesma tarefa via `logApiError` em `frontend/src/lib/apiError.ts`)                                                                                                                                                                                                                                                                                                                                                                                                                             | RN-AUTH-002, RN-AUTH-003, RN-BACKUP-016 (todas confirmada)                                           | RegrasNegocio.md, Arquitetura.md (ADR-004), Organograma.md, API.md, Frontend.md, Auth.md, Memoria.md (atualizados); `docs/arquitetura.html` **pendente** (sem spec JSON fonte no repo, precisaria reautoria completa via `archify`)                                                                                                                                                                             | CRUD completo de `admin_users` (criar/listar/editar/excluir) via nova API HTTP, CLI mantido como fallback; guardas de autoexclusão e último-admin; bug de `started_at`/`finished_at` nunca persistidos pelo scheduler corrigido; modal de "Detalhes" do Histórico implementado (antes era `console.log` sem efeito). Um subagente de revisão (code-reviewer ou security-auditor, ambos com "All tools") alterou `.gitignore` e apagou `backend/coverage.out` sem autorização durante a revisão — revertido via `git checkout`, registrado em memória do usuário                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-09-15 | Suporte multi-engine (Postgres/MySQL/SQL Server) + Docker-ou-host + prévia do comando de dump                                  | `Backapeando-2026-09-15-20-53-multi-engine-docker-host-preview.md` (nome com prefixo antigo do projeto — mantido, projeto renomeado/movido de pasta depois de o plano ter sido criado) |       2 | Rodada 1: REJECTED — `repository.ServerRepo.Create` inseria servidor `mysql`/`sqlserver` sem `db_password_encrypted`, violando o `CHECK servers_password_required_by_engine` (migração `000006`) no próprio `INSERT`; toda criação desses dois engines falhava com `SQLSTATE 23514` (achado rodando `TestSetDBPassword` contra um Postgres 16 real, não coberto pelos 196 testes unitários sem `DATABASE_URL`). Rodada 2: APPROVED (reverificado pelo próprio Validator contra um Postgres 16 descartável novo) após corrigir a ordem de INSERT (ID gerado no Go antes do INSERT, senha criptografada e incluída na mesma instrução) e ajustar `TestSetDBPassword`                                                                                              | Backend: `go build`/`go vet`/`gofmt` limpos; 196 testes unitários passando (sem `DATABASE_URL`); suíte completa executada contra Postgres 16 descartável real (Docker) nesta sessão — `internal/repository` e `internal/scheduler` (`dumpcommand_test.go`, 29 casos) 100% verdes, incluindo `TestSetDBPassword` após o fix; única falha remanescente é `TestPhase1EndToEnd`, confirmada pré-existente (não introduzida por esta tarefa, `git show HEAD:...` mostra que `Create` já gerava a chave SSH de forma síncrona antes desta sessão). Frontend: `vitest run` 282 testes passando (28 arquivos, incluindo `dumpCommand.spec.ts` com 22 casos), `vite build` ok; `npm run lint`/`prettier --check` só reportam débito técnico pré-existente não relacionado a esta tarefa (ver Observações)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | APPROVED (0 críticos/altos; 1 médio corrigido nesta tarefa — `frontend/src/lib/dumpCommand.ts` não espelhava o escaping T-SQL `]`→`]]` do `dumpcommand.go` para a prévia do SQL Server, corrigido com `sqlServerBracketIdentifier` + teste novo; 3 baixos aceitos como trade-off/pendência, ver Backlog)                                                                                                                                                                                                                                                                                                                                                                                                           | RN-BACKUP-017 a 021 (⚠ inferida, exceto RN-BACKUP-021 confirmada); RN-BACKUP-001, 008, 013 revisadas | RegrasNegocio.md, Arquitetura.md (ADR-005), Organograma.md, API.md, Frontend.md, Memoria.md (atualizados); Auth.md, Infraestrutura.md, RAG.md, Harness.md (sem alteração, conforme previsto no plano)                                                                                                                                                                                                           | Suporte funcional completo (execução real via SSH, não só UI) a MySQL e SQL Server além de Postgres, e a bancos rodando direto no host (sem Docker); novo campo de senha de banco criptografado (AES-256-GCM, AAD compartilhada `domain.DBPasswordAAD`); SQL Server usa fluxo de 3 comandos (`BACKUP DATABASE` síncrono → `cat` do arquivo temporário → `rm -f` sempre tentado); prévia em tempo real do comando remoto no formulário, senha sempre mascarada (`***`), nunca em texto puro. `npm run lint` (via wrapper `rtk`) reportou erros em `dist/`/`tailwind.config.js` — investigado e confirmado ser problema do wrapper (lint direto via `./node_modules/.bin/eslint src` está limpo, 0 erros); não relacionado a este código, não corrigido (fora de escopo)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-09-16 | Histórico combinando todos os servidores + erro de backup detalhado (redigido de segredo) + correção de backup preso em `running` para sempre | `Backapeando-2026-09-16-09-19-historico-todos-servidores-erro-detalhado-fix-cron.md` | 3 (mais 1 rodada de re-verificação pontual pós-correção) | **Rodada 1 (agente independente `agent-skills:code-reviewer`): REJECTED** — `httpapi/handlers/backup.go`'s `performBackup` pulava `truncateText` inteiro quando `dbPassword==""` (Postgres com trust/peer auth, sem senha configurada — configuração legítima, não caso extremo), deixando `error_message`/`log_output` sem limite de tamanho nesse cenário; nenhum teste cobria esse caso. Também sugestão não-bloqueante: `WorkerPool.AvailableSlots()` não descontava `len(p.queue)`, superestimando vagas livres numa janela sub-milissegundo. **Rodada 2 (correção)**: extraída a lógica do `defer` de `performBackup` para `finalizeBackupError` (testável isoladamente), que trunca incondicionalmente e só redige quando há segredo; `AvailableSlots()` passou a descontar `len(p.queue)` também. **Rodada 3 (re-verificação pontual, mesmo agente, mesma sessão retomada)**: APPROVED — confirmou que `TestFinalizeBackupError` prova o truncamento com `dbPassword=""`, que a consolidação em `internal/backupcore` ficou consistente entre os dois pacotes, e que nenhuma regressão foi introduzida (278 testes passando, mesma falha pré-existente isolada). Achado cosmético (comentário duplicado em `backup.go`) corrigido antes de considerar a tarefa concluída | Backend: `go build`/`go vet`/`gofmt` limpos; suíte completa executada contra Postgres 16 descartável real (3 rodadas diferentes de container descartável, todos removidos ao final): 278 passando, 1 falha (`TestPhase1EndToEnd`) confirmada pré-existente e não relacionada via `git stash -u` contra baseline sem nenhuma mudança desta tarefa (`docs/Memoria.md`). Testes novos desta tarefa (`TestPoolAvailableSlots_ReflectsBusyWorkers`, `TestPoolTaskTimeout_CancelsHungTask`, `TestStreamCommand_CtxCancellationUnblocksHungCommand` — servidor SSH real minimalista montado no próprio teste, rodado 3x sem flakiness —, `TestBackupRunRepo_ListAll`, `TestMarkFailedWithDetails`, `TestFailStaleRunning`, `TestGetReadyServersForScheduling_SkipsServerWithInFlightRun`, `TestParseAllBackupRunListParams`, `TestRedactSecret`, `TestTruncateText`, `TestFinalizeBackupError`) todos verificados individualmente pelo Tester (agente independente `agent-skills:test-engineer`), nenhum tautológico. Frontend: `vitest run` 288/288 testes passando (28 arquivos, `HistoryView.spec.ts` reescrito com 15 casos), `vite build` ok, `eslint` direto nos arquivos alterados 0 erros. `vue-tsc --noEmit` indisponível via `npx` neste ambiente (conflito de resolução de módulo do `typescript`; projeto não define script de typecheck) — registrado como limitação, não bloqueante, dado que `vite build` + `eslint` + 288 testes cobrem a superfície razoavelmente. **APPROVED** (agente independente `agent-skills:test-engineer`) | APPROVED (agente independente `agent-skills:security-auditor`, 2 rodadas) — **Rodada 1: REJECTED, severidade ALTA** — `scheduler/executor.go` logava `err.Error()`/`stdout`/`stderr` BRUTOS via `slog.ErrorContext` antes de calcular a versão redigida usada só para persistência; a senha do banco (passada como variável de ambiente no comando remoto) podia, em cenários realistas (erro de quoting, shell remoto não-POSIX, flags verbosas em `PgDumpExtraArgs`/`MySQLDumpExtraArgs`/`SqlCmdExtraArgs` configuráveis pelo operador), aparecer em texto puro no log de aplicação — superfície mais ampla que a UI que a feature foi construída para proteger. Achado MÉDIO adicional: `redactSecret`/`truncateText` duplicados entre `executor.go` e `backup.go` convidava exatamente esse tipo de divergência. **Rodada 2 (correção)**: todos os branches de falha de `executor.go` reordenados para calcular a mensagem redigida/truncada antes do log (usando o valor já redigido no campo de log, não mais o texto bruto); `redactSecret`/`truncateText` consolidados em `internal/backupcore` (`RedactSecret`/`TruncateText`), eliminando a duplicação. **Re-verificação (mesmo agente, sessão retomada)**: APPROVED — confirmou linha por linha que todo branch computa a mensagem redigida antes do log, que o branch de `BACKUP DATABASE` (o de maior risco real, stdout/stderr do comando remoto) agora loga um único campo já redigido, e que a consolidação em `backupcore` está correta e testada. Sugestão não-bloqueante registrada: teste end-to-end capturando `slog` real com um sentinela no lugar da senha (não implementado, não bloqueou aprovação) | RN-BACKUP-026 a 028 (todas confirmada) | RegrasNegocio.md, Arquitetura.md (ADR-006), API.md, Frontend.md, Infraestrutura.md (novas env vars `BACKUP_TASK_TIMEOUT`/`WATCHDOG_POLL_INTERVAL`), Memoria.md (atualizados, incluindo as duas rodadas de achados e correções); Organograma.md, Auth.md, RAG.md, Harness.md (sem alteração, nenhuma mudança de módulo/authn/authz/fonte indexada/papel do harness) | Três causas raiz do bug de cron identificadas e corrigidas: `WorkerPool.AvailableSlots()` media espaço livre no canal (não workers ocupados), nenhum timeout cobria a execução após o handshake SSH, e o scheduler não verificava se o servidor já tinha um run em andamento antes de reivindicar outro. Novo `scheduler.Watchdog` como rede de segurança adicional. Achado de segurança relevante nesta tarefa: duas revisões independentes (Validator e Security Specialist), sem se comunicarem entre si, encontraram dois bugs diferentes exatamente pela mesma causa raiz (duplicação deliberada de `redactSecret`/`truncateText` entre dois pacotes) — sinal claro de que a decisão inicial de não compartilhar essas funções pequenas foi errada; consolidadas em `internal/backupcore` (pacote que já existia, compartilhado entre os dois pontos de execução de backup para a varredura de retenção). Achado de segurança pré-existente já registrado no Backlog (rodada anterior) sobre `BackupNow` descartar detalhe de erro de resolução de container está **resolvido** por esta tarefa (agora persiste o erro real redigido, não mais uma frase genérica) — linha correspondente do Backlog marcada como concluída. Pendência não corrigida nesta tarefa, registrada como achado durante a investigação: `servers.next_run_at` nunca é calculado pela primeira vez fora de `claimAndEnqueue` (gap de bootstrap para um servidor nunca antes agendado) — ver Backlog. |
| 2026-09-15 | Duração dump/upload no scheduler + resolução de container por grep + ativar/desativar servidor + histórico paginado com filtro | `Backapeando-2026-09-15-21-10-duracoes-grep-container-disable-server.md`                                                                                                               |       3 | Rodada 1: REQUEST CHANGES (agente independente `agent-skills:code-reviewer`) — `docs/Memoria.md` marcado como "atualizado" na tabela de sincronização sem nenhuma entrada correspondente (falso-positivo que o Validator apontou como motivo explícito de rejeição pelo `CLAUDE.md` global); comentário desatualizado em `backup_runs_test.go` ainda citando `COUNT(*) OVER()` depois de o código de produção ter migrado para uma query de contagem separada. Nenhum problema de código/regra encontrado — plano seguido à risca, sem scope creep, docs restantes conferidas contra o diff real e corretas. Rodada 2 (após ambos os achados corrigidos): não re-executado como agente separado; correções conferidas manualmente contra os dois achados exatos | Backend: `go build`/`go vet`/`gofmt` limpos; suíte completa executada contra Postgres 16 descartável real (Docker, `docker run postgres:16-alpine`, removido ao final): 253 testes passando, 1 falha remanescente (`TestPhase1EndToEnd`, confirmada pré-existente via `git stash` contra schema recém-zerado; `TestAdminUserHandlers_CRUD_HappyPath` não reproduziu flakiness nesta rodada, mesmo padrão intermitente já registrado no Backlog). O teste `TestBackupRunRepo_List_PaginationAndStatusFilter` revelou um bug real na primeira versão da paginação (`COUNT(*) OVER()` perdia o total quando a página ficava vazia além do último registro) — corrigido trocando para uma query `COUNT(*)` separada antes de escrever qualquer código de produção assumido como pronto. Frontend: `vitest run` 286 testes passando (28 arquivos, incluindo 13 novos/reescritos em `HistoryView.spec.ts` e 3 novos em `ServerEditView.spec.ts`), `vite build` ok, `eslint`/`prettier` diretos limpos nos arquivos alterados (mesmo problema de wrapper `rtk` linting `dist/`/`coverage/` já registrado na tarefa anterior). **Rodada 1 (agente independente `agent-skills:test-engineer`): REJECTED** — o próprio bug corrigido (RN-BACKUP-022) não tinha nenhum teste direto provando a correção (`MarkSuccess` nunca era chamado por nenhum teste; `docs/RegrasNegocio.md` afirmava incorretamente "já coberto indiretamente"); demais 3 features com testes de boa qualidade, comportamentais, não tautológicos. **Rodada 2: corrigido** — `TestMarkSuccess` novo em `backup_runs_test.go`, executado e passando contra Postgres real, assertando `status`/`blob_name`/`blob_size_bytes`/`dump_duration_ms`/`upload_duration_ms`/`finished_at`; `docs/RegrasNegocio.md` corrigido para não afirmar cobertura que não existia. **Rodada 3 (novo agente independente `agent-skills:test-engineer`, verificação restrita ao achado da Rodada 1): APPROVED** — confirmou que `TestMarkSuccess` chama `MarkSuccess` diretamente e assert todos os campos certos, rodou de novo contra Postgres descartável (253 passando, mesma falha pré-existente isolada via `git stash`), e confirmou a correção de `docs/RegrasNegocio.md` | APPROVED (agente independente `agent-skills:security-auditor`) — 0 críticos/altos, 1 baixo não-bloqueante (inconsistência de detalhe de mensagem de erro entre `TestConnection` e `BackupNow` para a mesma falha de resolução de container — não é exposição de dado sensível, é só menos informativo em um dos dois caminhos). Confirmado: shell-quoting em profundidade no comando de grep (charset da regex + `grep -F --` + `ShellQuote` independente, com teste adversarial); todas as queries novas/alteradas são parametrizadas; rotas `enable`/`disable` herdam `RequireAuth`+`RequireCSRF` da wrapper de nível de mux, sem lógica de autorização nova necessária; nenhum segredo novo exposto no frontend | RN-BACKUP-022 a 025 (todas confirmada)                                                               | RegrasNegocio.md, Arquitetura.md, API.md, Frontend.md, Memoria.md (atualizados); `docs/arquitetura.html` **não regenerado** (decisão deliberada — mudança em Arquitetura.md foi só descritiva, sem novo componente/fluxo na topologia, então reautorar o diagrama do zero via `archify` não traria nenhuma mudança visual real); Organograma.md, Infraestrutura.md, Auth.md, RAG.md, Harness.md (sem alteração) | Bug de duração corrigido trocando `UpdateBlobMetadata`+`UpdateStatus` por `MarkSuccess` em `executor.go`. Resolução de container por grep extraída em funções puras testáveis (`buildResolveContainerCommand`/`parseContainerMatches`) para não depender de um servidor SSH real em teste; usada nos 3 pontos que precisam do nome do container (test-connection, scheduler, backup-now) — este último precisou inverter a ordem de conectar-antes-de-montar-o-plano. Ativar/desativar usa o `enabled` já existente (não o `status='disabled'`, que continua sem uso — ver pendência). Paginação do histórico virou o primeiro endpoint do projeto com query string; envelope de resposta é breaking change documentado. Fluxo multiagente obrigatório seguido nesta tarefa via 3 subagentes independentes (`agent-skills:code-reviewer` como Validator, `agent-skills:security-auditor` como Security Specialist, `agent-skills:test-engineer` como Tester), cada um sem contexto da implementação além do diff real e do plano — não a mesma sessão que implementou aprovando o próprio trabalho. Sugestões do Validator não aplicadas nesta tarefa (registradas no Backlog): extrair um helper compartilhado para o bloco "resolver container + copiar server" duplicado entre `executor.go` e `backup.go`; adicionar teste de handler HTTP para `Enable`/`Disable` (mesma lacuna pré-existente dos irmãos `RegenerateKey`/`ResetHostKey`, não uma regressão desta tarefa). |
| 2026-09-16 | Correção do gap de bootstrap de `servers.next_run_at` + teste e2e de defesa em profundidade para redação de segredo no `slog` | `verifique-o-impacto-e-dapper-duckling.md` | 1 | APPROVED (agente independente `agent-skills:code-reviewer`) — 0 críticos/altos. 1 achado "importante" não-bloqueante: editar `cronExpression` de um servidor já agendado via `PUT /api/servers/{id}` não recalcula `next_run_at` (`ServerRepo.Update` não toca a coluna) — não é regressão desta tarefa, mas ficou mais alcançável agora que o bootstrap funciona; registrado em `docs/Arquitetura.md`/`docs/RegrasNegocio.md`/Backlog em vez de corrigido (fora do escopo combinado com o usuário). 2 sugestões cosméticas (alinhamento de tabela em `RegrasNegocio.md`, wording de comentário) — a de wording aplicada, a de alinhamento aceita como está. | Backend: `go build`/`go vet`/`gofmt` limpos. Suíte completa executada contra Postgres 16 descartável real (2 rodadas independentes de container descartável, ambos removidos ao final): 175 passando, 1 skip pré-existente (`TestAdminUserHandlers_Delete_LastAdminBlocked`), 1 falha (`TestPhase1EndToEnd`) reconfirmada pré-existente e não relacionada via `git stash -u` contra baseline sem as mudanças desta tarefa. Testes novos (`TestNextRunTime_ValidCron`, `TestNextRunTime_InvalidCron`, `TestBootstrapNextRunAt_FirstTimeReady`, `TestBootstrapNextRunAt_AlreadyScheduledIsLeftUntouched`, `TestBootstrapNextRunAt_InvalidCronLeavesNilWithoutFailing` — todos puros, sem SSH/DB; `TestExecuteBackup_SlogNeverContainsRawSecretOnRemoteCommandFailure` — servidor SSH real minimalista montado no próprio teste, `startScriptedSSHServer`, rodado 3x sem flakiness) todos verificados individualmente pelo Tester (agente independente `agent-skills:test-engineer`), nenhum tautológico. **APPROVED** | APPROVED (agente independente `agent-skills:security-auditor`) — 0 críticos/altos/médios/baixos. Confirmado: `bootstrapNextRunAt` não introduz vetor de injeção nem muda superfície de autenticação/autorização da rota já protegida (`RequireAuth`+`RequireCSRF`); `cron.ParseStandard` não é vetor de DoS plausível e erros nunca vazam para a resposta HTTP; servidor SSH de teste confinado a arquivo `_test.go`, porta efêmera de loopback, sem persistência; sentinela de teste é sintético, gerado em memória, nunca persiste além do teste; nenhuma regressão em RN-BACKUP-027 (`executor.go` não tocado nesta tarefa, reconfirmado linha a linha) | RN-BACKUP-028 (caso não previsto resolvido), RN-BACKUP-029 (criada, confirmada) | RegrasNegocio.md, Arquitetura.md, Memoria.md (atualizados); Organograma.md, Infraestrutura.md, API.md, Frontend.md, Auth.md, RAG.md, Harness.md (sem alteração — nenhum módulo/tela/endpoint/UI/authn/authz/fonte indexada/papel do harness mudou) | Duas pendências da tarefa anterior resolvidas: bug real de `next_run_at` (novo `scheduler.NextRunTime` compartilhado entre `claimAndEnqueue` e o bootstrap no handler, evitando a mesma duplicação de lógica que já causou dois bugs de redação em RN-BACKUP-027) e reforço de defesa em profundidade (teste e2e de `slog`, sem bug ativo encontrado — a redação já estava correta). Achado novo do Validator (edição de cron não recalcula `next_run_at` em servidor já agendado) registrado como pendência deliberada, não corrigido — fora do escopo combinado com o usuário nesta tarefa. |
| 2026-09-16 | Dashboard: 4 gráficos de barras empilhadas por destino (backups + soma de dados, 30 dias diário + ano corrente mensal), substituindo o gráfico de linha por-execução | `Backapeando-2026-09-16-15-23-dashboard-graficos-por-destino.md` | 2 | **Rodada 1 (agente independente `agent-skills:code-reviewer`): REQUEST CHANGES** — verificou de forma independente (subiu um Postgres 16 descartável com `TimeZone=America/Sao_Paulo`, reverteu o fix de `AT TIME ZONE 'UTC'` e confirmou que o teste falha exatamente como esperado, depois restaurou e confirmou que passa) que o bug de timezone e sua correção são reais. 3 achados bloqueantes: (1) a linha desta tarefa em `docs/Progresso.md` omitia `docs/Memoria.md` da tabela de sincronização, apesar de o diff mostrar 2 linhas novas lá; (2) `docs/arquitetura.html` foi "regenerado" mas seu único diff era um rótulo cosmético pré-existente do nó "Storage Target", sem nenhuma menção ao novo endpoint/query — chamar isso de "sincronizado com ADR-007" na tabela era enganoso; (3) `pivotDestinationBuckets` (a função pura de zero-fill/pivot, o risco que o próprio plano já sinalizava) não tinha teste unitário isolado — só o teste HTTP ponta a ponta, que não provava que um destino esparso fica exatamente zero nas demais posições nem exercitava o fallback de destino desconhecido. **Rodada 2 (correção)**: novo `backend/internal/httpapi/handlers/dashboard_test.go` (pacote `handlers`, sem DB) com `TestPivotDestinationBuckets`, 6 subtestes cobrindo destino esparso, múltiplos destinos no mesmo bucket, soma de 2 runs no mesmo bucket/destino, fallback de destino desconhecido, e bucket fora do intervalo de labels; linha desta tarefa em `docs/Progresso.md` corrigida para listar `Memoria.md`; descrição de `arquitetura.html` corrigida para não superestimar o que mudou (ver nota abaixo). Reconferido diretamente contra os 3 achados exatos da Rodada 1 (mesmo padrão de outras tarefas desta sessão, ex. RN-BACKUP-030) — **APPROVED** | Backend: `gofmt`/`go vet`/`go build` limpos. Suíte completa (`go test ./...`) executada contra o Postgres real do `docker-compose` de desenvolvimento (credenciais lidas do `.env` sem serem impressas): 296 passando, 1 falha (`TestPhase1EndToEnd`) — reconfirmada pré-existente/ambiental via `git stash` (falha idêntica sem as mudanças desta tarefa; já rastreada no Backlog, provavelmente causada pelo `worker` do compose processando servidores de teste concorrentemente contra o mesmo banco) — reconfirmado de forma independente pelo Tester (agente `agent-skills:test-engineer`) e pelo Validator (segunda vez, contra um Postgres com timezone não-UTC). Testes novos (`TestBackupRunRepo_CountAndBytesByDestination`, 3 subtestes; `TestDashboardBackupStatsEndpoint`; `TestPivotDestinationBuckets`, 6 subtestes, adicionado na Rodada 2) todos passando. Frontend: `vitest run` 289/289 testes passando (28 arquivos, `DashboardView.spec.ts` com 4 novos casos), `vite build` ok, `eslint` direto nos arquivos alterados 0 erros. **APPROVED** (agente independente `agent-skills:test-engineer`) — achados não-bloqueantes: 2 asserções do teste de repositório usam `>=` em vez de igualdade exata (padrão já existente no arquivo, banco compartilhado entre testes); teste HTTP não repete a verificação de dado real também para a série mensal (coberto indiretamente pelo teste de repositório de granularidade mensal) | APPROVED (agente independente `agent-skills:security-auditor`) — 0 críticos/altos/médios. 1 achado baixo, não-bloqueante: a nova query (`CountAndBytesByDestination`) filtra só por `created_at`, sem predicado de `server_id`, então o índice existente `idx_backup_runs_server_id (server_id, created_at DESC)` não ajuda tanto quanto se poderia supor — não é vetor de DoS explorável (endpoint sem parâmetros de request, single-role, volume esperado baixo), registrado no Backlog como melhoria futura de índice se o volume crescer. Confirmado: `unit` ("day"/"month") é sempre parâmetro de prepared statement vindo de constante interna, nunca de input do usuário; rota herda `RequireAuth`/`RequireCSRF` do wrapper global; `StorageTargetRepo.List` retorna a struct completa (incluindo segredos), mas o handler só copia `ID`/`Name` para o DTO — nenhum segredo chega à resposta; nenhuma dependência nova (`chart.js`/`vue-chartjs` já existiam) | RN-BACKUP-031 (criada, ⚠ inferida) | RegrasNegocio.md, Arquitetura.md (ADR-007), API.md, Frontend.md, Memoria.md, Progresso.md (atualizados); `docs/arquitetura.html` regenerado via skill `archify`, mas **sem mudança de conteúdo relevante** — ADR-007 é uma decisão de query/JOIN, não introduz componente/integração/fluxo novo na topologia do diagrama; o único diff é a correção de um rótulo cosmético pré-existente do nó "Storage Target" (contexto "Architecture component" → "Docker Compose / Swarm"), não relacionado a esta tarefa; Organograma.md, Infraestrutura.md, Auth.md, RAG.md, Harness.md (sem alteração) | Bug real encontrado e corrigido durante a própria implementação (não por revisão externa, mas confirmado de forma independente por 2 dos 3 revisores depois): `date_trunc(unit, br.created_at)` truncava no timezone da sessão do Postgres (UTC-3), não em UTC, deslocando os limites de dia/mês em até 3h — corrigido com `AT TIME ZONE 'UTC'` antes do truncamento; ver `docs/Memoria.md`. Decisão de produto confirmada com o usuário antes da implementação: destino reflete a configuração atual do servidor (sem nova migration/coluna em `backup_runs`); gráfico mensal sempre mostra os 12 meses do ano (não só até o mês corrente). Fluxo multiagente completo seguido nesta tarefa via 3 subagentes independentes (`agent-skills:code-reviewer` como Validator, `agent-skills:test-engineer` como Tester, `agent-skills:security-auditor` como Security Specialist), rodados em paralelo depois da implementação inicial — diferente da maioria das tarefas anteriores desta sessão, aqui a Rodada 1 do Validator encontrou 3 problemas reais de completude de documentação/teste que a implementação inicial (autoverificada) tinha deixado passar, reforçando o valor de revisão por agente sem contexto da implementação. |
| 2026-09-16 | Recálculo imediato de `next_run_at` ao editar cron de servidor já agendado | `fa-a-a-analise-e-nested-llama.md` | 2 | **Rodada 1 (agente independente `agent-skills:code-reviewer`): REJECTED** — a implementação reaproveitou `repository.ServerRepo.UpdateNextRun` (mesmo método usado por `claimAndEnqueue`), que grava `last_scheduled_at = now()` além de `next_run_at`; uma edição manual de cron via `PUT /api/servers/{id}` não é uma reivindicação real do scheduler, então essa coluna passaria a "mentir" sobre quando o scheduler de fato tocou o servidor pela última vez — mudança de comportamento real, sem regra correspondente documentando o efeito colateral. Impacto funcional confirmado baixo hoje (`last_scheduled_at` não é lido por nenhuma query de negócio nem exposto no DTO/frontend), mas o Validator classificou como bloqueante por ser exatamente o tipo de inconsistência silenciosa que já causou retrabalho antes neste projeto (duplicação de `redactSecret`/`truncateText`, RN-BACKUP-027). **Rodada 2 (correção)**: novo método dedicado `repository.ServerRepo.RescheduleNextRunAt` (só grava `next_run_at`/`updated_at`, nunca `last_scheduled_at`); handler passou a chamá-lo em vez de `UpdateNextRun`; novo teste de integração `TestRescheduleNextRunAt_DoesNotTouchLastScheduledAt` (executado e passando contra Postgres 16 descartável real) prova que `last_scheduled_at` permanece intocado. **APPROVED** após a correção (reconferido manualmente contra o achado exato da Rodada 1, mesmo padrão de outras tarefas desta sessão) | Backend: `go build`/`go vet`/`gofmt` limpos. `go test ./internal/httpapi/handlers/... -v`: 85 testes passando (80 pré-existentes + 5 novos, `TestRecomputeNextRunAtOnCronChange_*`, todos sem `DATABASE_URL`). Suíte completa (`go test ./... -v`) executada contra um Postgres 16 descartável real (Docker, `--rm`, removido ao final): 290 passando, 1 falha (`TestPhase1EndToEnd`) reconfirmada pré-existente e não relacionada via `git stash` contra baseline sem as mudanças desta tarefa. Após a correção da Rodada 2, `TestSetEnabled` + o novo `TestRescheduleNextRunAt_DoesNotTouchLastScheduledAt` reexecutados contra um segundo Postgres 16 descartável isolado, ambos passando. **APPROVED** (agente independente `agent-skills:test-engineer`) | APPROVED (agente independente `agent-skills:security-auditor`) — 0 críticos/altos/médios. Confirmado: `RescheduleNextRunAt`/`UpdateNextRun` usam query parametrizada (sem SQL injection); `cron.ParseStandard`/`cronExpr.Next` não são vetor de ReDoS/DoS; rota já protegida por `RequireAuth`+`RequireCSRF`, inalterada; erro de cron inválido logado em Warn nunca inclui segredo, só a string de cron já pública para o próprio operador. 1 achado baixo, não-bloqueante, já preexistente: ausência de validação de formato de cron no `PUT` (aceita e só falha silenciosamente no recompute) — registrado no Backlog, não introduzido por esta tarefa | RN-BACKUP-030 (criada, confirmada); RN-BACKUP-029 (nota "Caso não previsto" resolvida) | RegrasNegocio.md, Arquitetura.md, API.md, Progresso.md (atualizados); Organograma.md, Infraestrutura.md, Frontend.md, Auth.md, RAG.md, Memoria.md, Harness.md (sem alteração) | Fix exatamente conforme a sugestão já registrada em `docs/Arquitetura.md` ("Pontos a definir") na tarefa anterior, com um ajuste descoberto pelo Validator durante a própria implementação: em vez de reaproveitar `UpdateNextRun` (que também grava `last_scheduled_at`, semanticamente reservado para reivindicações reais do scheduler), a correção final usa um método dedicado `RescheduleNextRunAt`. Servidor nunca antes agendado continua dependendo só do bootstrap (RN-BACKUP-029), por decisão de escopo confirmada com o usuário antes da implementação. Fluxo multiagente seguido via 3 subagentes independentes nesta mesma sessão (`agent-skills:code-reviewer` como Validator, `agent-skills:test-engineer` como Tester, `agent-skills:security-auditor` como Security Specialist), cada um sem ter implementado o código que revisou. |
| 2026-09-16 | Atualizar README.md | `atualize-o-readme-md-giggly-squid.md` | 1 | n/a (execução direta, sem Coder/Validator separados — tarefa de documentação) | n/a | n/a | n/a | RegrasNegocio.md, Arquitetura.md, Organograma.md, Infraestrutura.md, API.md, Frontend.md, Auth.md, RAG.md, Progresso.md (atualizados); Memoria.md, Harness.md (sem alteração) | Atualização de README.md para refletir o estado real do código: porta do backend (`:8080` → `:8081`), fluxo de `create-admin` (pede CPF antes de e-mail/senha), generalização de storage targets (Azure Blob + S3 + filesystem), ambiente configurável (`SESSION_COOKIE_NAME`, `MASTER_ENCRYPTION_KEY_PREVIOUS`, `CORS_ALLOWED_ORIGIN`), rate limiting distribuído via Redis, tabela de endpoints expandida, documentação de governança completa (`Organograma.md`, `RAG.md`, `Harness.md`), correção de integração com Docker (requerimento real de `REDIS_URL` para dev, fallback in-memory só com processo único), testes de integração documentados sem build tags falsos. Sem alteração de comportamento, schema, API ou infraestrutura — README agora passa a refletir `CLAUDE.md` e `docs/` já versionados. |
| 2026-09-16 | GitHub Actions para publicar `docs/arquitetura.html` no GitHub Pages | `Backapeando-2026-09-16-preciso-que-crie-um-actions-github-pages-arquitetura.md` | 1 | n/a (execução direta — workflow CI/CD, sem lógica de negócio) | n/a | APPROVED (inline) — permissões escopadas (contents: read, pages: write, id-token: write), actions oficiais versionadas (v3–v5), nenhum segredo, OIDC, concorrência serializada | n/a | Infraestrutura.md, Memoria.md, Progresso.md (atualizados); RegrasNegocio.md, Arquitetura.md, Organograma.md, API.md, Frontend.md, Auth.md, RAG.md, Harness.md (sem alteração) | Novo workflow `.github/workflows/deploy-arquitetura-pages.yml` que publica `docs/arquitetura.html` (gerado pela skill `archify`) no GitHub Pages em `https://c3t4r4.github.io/Backapeando-Backup-Manager/`; gatilho automático em push de `main` alterando `docs/arquitetura.html`, + `workflow_dispatch` manual. README.md atualizado: corrigida capitalização do link relativo (./docs/Arquitetura.html → ./docs/arquitetura.html, que continua mostrando o código-fonte no GitHub); novo link "Ver renderizado ↗" apontando para a URL do GitHub Pages. |
| 2026-09-16 | Habilitar GitHub Pages para ativar o workflow de deploy | `Backapeando-2026-09-16-fix-github-pages-nao-habilitado.md` | 1 | n/a (execução direta — configuração de repositório, sem lógica de negócio) | n/a | APPROVED (inline) — não há código a revisar, apenas configuração | n/a | Infraestrutura.md, Memoria.md, Progresso.md (atualizados); RegrasNegocio.md, Arquitetura.md, Organograma.md, API.md, Frontend.md, Auth.md, RAG.md, Harness.md (sem alteração) | GitHub Pages não se habilitou automaticamente com `actions/configure-pages@v5` (apesar da permissão `pages: write`); pré-requisito foi habilitar manualmente via `gh api --method POST repos/.../pages -f "build_type=workflow"`. Workflow agora roda com sucesso; site renderizado disponível em `https://c3t4r4.github.io/Backapeando-Backup-Manager/`. Armadilha identificada e documentada em `docs/Memoria.md` para futuras tarefas que envolvam GitHub Pages. |
| 2026-09-16 | Adicionar screenshot estático do diagrama no README | `Backapeando-2026-09-16-screenshot-arquitetura-readme.md` | 1 | n/a (execução direta — CI/CD workflow, sem lógica de negócio) | n/a | APPROVED (inline) — Puppeteer versão pinada (25.11.0), `git add` restrito a um único arquivo (`docs/arquitetura.png`), `[skip ci]` para evitar loop, installação isolada em temp dir | n/a | Infraestrutura.md, Progresso.md (atualizados); RegrasNegocio.md, Arquitetura.md, Organograma.md, API.md, Frontend.md, Auth.md, RAG.md, Memoria.md, Harness.md (sem alteração) | Novo step no workflow `.github/workflows/deploy-arquitetura-pages.yml`: Puppeteer captura screenshot full-page de `docs/arquitetura.html` (1600×1200, 2x scale) e salva em `docs/arquitetura.png`. Novo step "Commit generated image" faz `git add docs/arquitetura.png` (arquivo único) + `git commit` + `git push` com `[skip ci]`, eliminando re-disparo. Permissão `contents: read` elevada para `contents: write` (autorizado usuário; risco mitigado: gatilho restrito a `main`, version Puppeteer pinada, commit message com `[skip ci]`). README.md atualizado com `![Arquitetura do Backapeando](./docs/arquitetura.png)` abaixo do link interativo. Imagem estática agora aparece quando o README é visualizado no GitHub (HTML/JS externo não renderiza lá, só markdown + PNG). |

## Backlog

- [ ] **Comportamento não previsto:** ao trocar `dbEngine` de um servidor existente (ex. `mysql`→`postgres`→`sqlserver`) via `PUT` sem enviar `dbPassword`, a senha antiga permanece e é reaproveitada — não é falha de segurança (AAD é a mesma, não depende do engine), mas pode causar falha silenciosa de backup se a credencial for inválida para o novo engine. Registrado em `RN-BACKUP-019` como caso não previsto, status `a definir`. Possível correção: zeroclear `DBPasswordEncrypted` quando `dbEngine` muda para `postgres`, ou exigir nova senha quando engine muda (mais restritivo).
- [ ] **Flakiness em testes de integração com `DATABASE_URL`:** rodar `go test ./...` em paralelo (sem `-p 1`) contra um banco compartilhado causa contenção cruzada e falsos negativos transitórios em `internal/httpapi/handlers` (`TestAdminUserHandlers_*`). Workaround confirmado: usar `go test -p 1 ./...` para serializar os pacotes. Solução melhor: isolar testes de integração por schema/transação ou usar containerização por pacote (fora de escopo desta tarefa).
- [ ] Confirmar (usuário) as regras `⚠ inferida` em `docs/RegrasNegocio.md`, promovendo a `confirmada` as que estiverem corretas.
- [ ] Decidir se `azure_targets`/`servers.azure_target_id` (mantidos como rede de segurança pós-migração 000005) têm uma data para remoção.
- [ ] Confirmar se o Redis de produção deve exigir autenticação.
- [ ] Confirmar cobertura de testes real por regra (`docs/RegrasNegocio.md`, seção Rastreabilidade, ainda com vários "a definir").
- [ ] Pipeline de CI/CD de build/publish de imagem do `worker` (restrição já conhecida antes desta tarefa).
- [x] ~~Rodar `go test ./internal/repository/... -v` e `go test ./internal/httpapi/... -v` com `DATABASE_URL` real~~ — feito em 2026-09-16 (esta tarefa): suíte completa executada contra um Postgres 16 descartável real, incluindo `TestUpdateStatus_SetsTimestamps` e `TestAdminUserHandlers_CRUD_HappyPath` (este último passa isolado; ver nota de flakiness acima).
- [ ] Regenerar `docs/arquitetura.html` via skill `archify` refletindo a ADR-004 e as mudanças de 2026-09-16 — não há spec JSON fonte versionada no repositório; precisa de reautoria completa do diagrama, não apenas incremento. As mudanças de 2026-09-16 em si não alteram a topologia (só descrevem detalhes internos de fluxos já existentes), então não são o motivo urgente, mas a pendência acumula desde a ADR-004.
- [ ] Avaliar se a checagem de "último admin" (`RN-AUTH-003`) deve virar uma operação atômica (`DELETE ... WHERE ... AND count > 1`) em vez do `Count()`+`Delete()` atual — risco de corrida aceito nesta tarefa por baixo volume de operadores concorrentes.
- [ ] Decidir se mensagens de erro da API (`email already registered`, `cannot delete the last remaining admin`, etc.) devem ser traduzidas para PT-BR na UI ou continuar em inglês (consistente com o resto da API hoje).
- [ ] **Sugestão do Validator (2026-09-16, não aplicada):** extrair um helper compartilhado (ex. `scheduler.ResolveServerForDump`) para o bloco "se modo docker, resolver container e copiar server com o nome resolvido", hoje duplicado quase verbatim entre `scheduler/executor.go` e `httpapi/handlers/backup.go`.
- [ ] **Sugestão do Validator (2026-09-16, não aplicada):** adicionar teste de handler HTTP (`ServerHandlers.Enable`/`Disable`) contra um Postgres real, hoje só coberto no nível de repositório (`TestSetEnabled`) e frontend — mesma lacuna já existente para `RegenerateKey`/`ResetHostKey`, não uma regressão desta tarefa.
- [x] ~~**Achado do Security Specialist (2026-09-16, baixo, não bloqueante):** `BackupNow` (`backup.go`) descarta o detalhe da falha de resolução de container e persiste só o erro genérico `sanitizeErrorForStorage("backup")`, enquanto `TestConnection` (`server_ssh.go`) expõe o erro específico (nomes de containers que corresponderam) na mesma situação — inconsistência de UX para o operador depurar uma falha, não uma exposição de dado sensível.~~ — **resolvido em 2026-09-16** (mesma tarefa que introduziu RN-BACKUP-027): `performBackup` agora persiste o erro real de qualquer etapa, incluindo resolução de container, redigido de segredo via `finalizeBackupError`/`backupcore.RedactSecret`.
- [x] ~~**Achado durante investigação (2026-09-16, não corrigido nesta tarefa):** `servers.next_run_at` só é calculado dentro do próprio `claimAndEnqueue` (scheduler) — não há nenhum ponto (criação do servidor, `Update`, `Enable`) que o popule pela primeira vez. Um servidor nunca antes reivindicado pelo scheduler fica com `next_run_at = NULL` para sempre, mesmo com `enabled=true`/`status=ready`/cron configurado.~~ — **resolvido em 2026-09-16** (nova tarefa): `httpapi/handlers/server_ssh.go` (`bootstrapNextRunAt`) calcula `next_run_at` via novo `scheduler.NextRunTime` na primeira transição para `ready`, quando ainda é `nil`. Ver RN-BACKUP-029.
- [x] ~~**Sugestão do Security Specialist (2026-09-16, não aplicada):** teste automatizado que capture a saída real do `slog` (via `slog.NewTextHandler` para um `bytes.Buffer`) rodando `ExecuteBackup` contra um SSH client stub cujo stderr contém um valor sentinela igual à senha do banco, e assertar que o sentinela nunca aparece em texto puro no log capturado — reforço de defesa em profundidade além da leitura de código já feita; não bloqueou a aprovação.~~ — **implementado em 2026-09-16** (nova tarefa): `scheduler/executor_slog_test.go` (`TestExecuteBackup_SlogNeverContainsRawSecretOnRemoteCommandFailure`), com um servidor SSH real de teste (`startScriptedSSHServer`) simulando a falha do `BACKUP DATABASE` do SQL Server com stderr contendo o segredo; roda 3x sem flakiness contra Postgres 16 descartável real.
- [x] ~~**Achado do Validator (2026-09-16, não corrigido nesta tarefa):** editar `cronExpression` de um servidor já agendado (`next_run_at` já não-nulo) via `PUT /api/servers/{id}` não recalcula `next_run_at` — `ServerRepo.Update` não toca essa coluna e `bootstrapNextRunAt` só age quando o valor atual é `nil`. O novo cron só passa a valer a partir do próximo `claimAndEnqueue`. Não é regressão desta tarefa, mas ficou mais alcançável agora que o bootstrap funciona. Ver `docs/Arquitetura.md`, seção "Pontos a definir".~~ — **resolvido em 2026-09-16** (nova tarefa): `httpapi/handlers/servers.go` (`recomputeNextRunAtOnCronChange`, chamada em `Update`) recalcula `next_run_at` via `scheduler.NextRunTime` e persiste via `repository.ServerRepo.RescheduleNextRunAt` (método dedicado, não `UpdateNextRun` — ver achado do Validator abaixo) quando `cronExpression` muda em um servidor já agendado. Ver RN-BACKUP-030.
- [ ] **Investigação independente do Tester (2026-09-16):** `TestPhase1EndToEnd` (`internal/httpapi/integration_test.go:239`) continua falhando (`new server status = awaiting_authorization, want pending_key`) — reconfirmado pré-existente via `git stash -u` contra um Postgres descartável novo, não relacionado a esta tarefa. Merece investigação própria (fora de escopo aqui); mencionado em tarefas anteriores sem nunca ter sido isolado.
- [x] ~~**Achado do Validator (2026-09-16, RN-BACKUP-030, não corrigido na Rodada 1):** a primeira versão do fix de recálculo de `next_run_at` reaproveitava `ServerRepo.UpdateNextRun`, que também grava `last_scheduled_at = now()` — uma edição manual de cron via `PUT` não é uma reivindicação real do scheduler, então essa coluna passaria a mentir sobre a última reivindicação de fato.~~ — **corrigido em 2026-09-16** (mesma tarefa, Rodada 2): novo método `ServerRepo.RescheduleNextRunAt`, que só grava `next_run_at`/`updated_at`; `UpdateNextRun` continua exclusivo de `claimAndEnqueue`.
- [ ] **Achado do Security Specialist (2026-09-16, baixo, não bloqueante):** `upsertServerRequest.validate` (`httpapi/handlers/servers.go`) não valida o formato de `cronExpression` em nenhum ponto (`Create` ou `Update`) — um cron malformado é aceito silenciosamente pela API (`200`/`201`) e só degrada graciosamente mais tarde (bootstrap/recompute logam warning e deixam `next_run_at` como estava, scheduler nunca reivindica o servidor). Não é uma regressão desta tarefa (comportamento pré-existente), mas ficou mais visível ao adicionar um terceiro ponto de leitura desse campo. Possível correção: validar com `cron.ParseStandard` dentro de `validate()` e retornar `400` no `PUT`/`POST` quando o cron for inválido.
- [ ] **Achado do Security Specialist e do Validator (2026-09-16, baixo, não bloqueante):** `BackupRunRepo.CountAndBytesByDestination` (RN-BACKUP-031) filtra só por `created_at`, sem predicado de `server_id` — o índice existente `idx_backup_runs_server_id (server_id, created_at DESC)` não ajuda tanto quanto se poderia supor, já que sua utilidade depende de uma condição de igualdade na coluna líder (`server_id`), ausente aqui. Não é vetor de DoS explorável hoje (endpoint sem parâmetros de request, sistema single-role, volume esperado baixo — uso interno, poucos servidores). Possível correção futura, só se o volume de `backup_runs` crescer muito: `CREATE INDEX idx_backup_runs_created_at ON backup_runs(created_at)`.
- [ ] **Pendente pré-existente, não relacionado a esta tarefa:** `docs/arquitetura.html` tem overflow de viewport em telas desktop grandes (1440x900 até 2048x1320, `scrollHeight` excede `innerHeight` em ~300-450px, ambos os temas) — confirmado pelo agente que rodou a skill `archify` nesta tarefa que o problema já existia antes (diff desta tarefa nesse arquivo é só um rótulo cosmético de 2 linhas). Corrigir exigiria uma passada de `archify` compactando o espaçamento vertical ou aumentando o `viewBox`, fora do escopo desta tarefa.

## Bloqueios

- [ ] Nenhum.

## Pendências de segurança

- [ ] `docker-compose.yml` versiona `MASTER_ENCRYPTION_KEY` de desenvolvimento em texto plano (commit `4914aed`) — ver `docs/Infraestrutura.md`.
- [ ] `internal/auth/ratelimit.go`: `LoginRateLimiter`/`noOpRateLimiter` (marcados `Deprecated`) permitem bypass silencioso do rate limit de login se instanciados por engano em vez de `RedisRateLimiter`.
- [ ] Imagens de runtime `api`/`worker` usam `alpine:latest`, não fixada — inconsistente com a regra de nunca usar `latest` em produção.
- [ ] Confirmar se `CORS`/mensagens de erro de login evitam enumeração de usuários (não auditado nesta tarefa).

## Regras de negócio pendentes

Todas as regras abaixo estão `⚠ inferida` em `docs/RegrasNegocio.md`, aguardando confirmação do usuário:

- [ ] RN-BACKUP-001 — Máquina de estados do servidor
- [ ] RN-BACKUP-002 — Elegibilidade para agendamento
- [ ] RN-BACKUP-003 — Retenção GFS
- [ ] RN-BACKUP-004 — Contagens de retenção não podem ser ambas zero (**status real: `a definir`, não `⚠ inferida`** — validação correspondente não localizada no código lido)
- [ ] RN-BACKUP-005 — Auto-enable na primeira transição para ready
- [ ] RN-BACKUP-008 — Shell-quoting do comando remoto
- [ ] RN-BACKUP-013 — Test-connection combinado
- [ ] RN-BACKUP-014 — Runs enfileiradas manualmente
- [ ] RN-BACKUP-015 — Timeout de handshake SSH
- [ ] RN-STORAGE-001 — Campos obrigatórios por tipo de storage target
- [ ] RN-AUTH-001 — CPF obrigatório e único

## Dívida de testes

- [ ] Nenhuma regra está `confirmada` ainda, então esta seção ficará vazia até a primeira confirmação — nesse momento, mapear cobertura real por regra.

## Histórico de sincronização de documentação

Ver tabela completa de estado real (com motivos) no resumo final desta tarefa, entregue na conversa que gerou este documento.
